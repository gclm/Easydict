# 任务模式

## 模式判断

- 用户要求方案、分析、解释或评估时，使用计划模式。
- 用户要求修改、修复、更新、实现或执行时，使用执行模式。
- 用户的禁止、范围和顺序要求优先；无法确定是否允许修改时，使用计划模式。

## 计划模式

- 只读取和检查现状，输出分析、建议或实施方案。
- 不修改仓库文件、Git 状态或外部服务。

## 执行模式

- 在用户指定范围内完成修改和必要验证。
- 完成并验证后自动创建本地提交；用户明确要求不提交时除外。没有差异时不创建空提交。
- push、创建 Pull Request、发布及其他外部写入仅在用户明确要求时执行。
